"use strict";
const addressesDictinary = require('./addressDictionary.js')
let portFilter = (Project) => {
    switch (Project) {
        case 1: return
        case 2: return
        case 3: return
        default: return
    }
}